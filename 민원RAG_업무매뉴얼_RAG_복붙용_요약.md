# 민원 RAG 업무매뉴얼 RAG 전환 보고 요약

## 1. 추진 목적

본 프로젝트는 화성시 징수과 직원이 업무매뉴얼, 법령, 지침, 판례·해석자료를 자연어로 검색하고, 등록된 문서 근거를 바탕으로 업무 절차를 확인할 수 있도록 지원하는 내부 업무보조 RAG 시스템 구축을 목적으로 추진하였다.

초기에는 일반 민원 안내 중심의 RAG를 검토하였으나, 실제 활용성을 높이기 위해 체납처분, 압류, 공매, 관허사업 제한 등 징수과 업무매뉴얼 중심의 RAG 시스템으로 전환하였다.

## 2. 유료 AI에서 무료 할당 AI로의 전환

초기 시스템은 OpenAI 유료 API를 사용하였다. 문서 임베딩에는 `text-embedding-3-small`, 답변 생성에는 `gpt-5-mini`를 사용하도록 구성되어 있었다. 그러나 문서 색인과 질문·답변이 반복될수록 API 비용이 발생하므로, 교육·연구 및 프로토타입 단계의 비용 부담을 낮추기 위해 Cloudflare Workers AI 기반으로 전환하였다.

전환 후 문서 임베딩 모델은 `@cf/baai/bge-m3`, 답변 생성 모델은 `@cf/qwen/qwen3-30b-a3b-fp8`으로 변경하였다. 인증도 OpenAI API Key 방식에서 Cloudflare Account ID와 Workers AI API Token 방식으로 변경하였다.

이 전환을 통해 유료 API 결제 없이 무료 할당 범위에서 실습과 초기 검증이 가능해졌다. 다만 Cloudflare 무료 할당은 일일 사용량 제한이 있으므로, 대량 자료를 반복 색인하면 한도가 소진될 수 있다. 실제 구축 과정에서도 10,000 neurons 무료 한도 소진으로 429 오류가 발생하였다.

따라서 무료 전환은 비용이 완전히 발생하지 않는다는 의미가 아니라, 소규모 연구·개발 단계에서 무료 할당을 활용하는 방식이다. 정식 운영 시에는 Cloudflare 유료 플랜, 다른 상용 API 또는 로컬 임베딩·로컬 LLM 도입을 검토할 필요가 있다.

## 3. 시스템 구성

PDF, HWPX, PPTX, TXT, RTF-DOC 문서를 읽어 텍스트를 추출하고 개인정보 형식을 마스킹한 후, 문서를 청크 단위로 분할하여 임베딩 벡터를 생성하였다. 사용자가 질문하면 관련 청크를 검색하고, Cloudflare Workers AI 답변 모델이 등록 문서 근거만 활용하여 답변을 생성하도록 구성하였다.

답변 화면에는 답변뿐 아니라 근거자료 파일명, 문서 분류, 페이지 정보와 검색된 원문을 함께 표시하도록 하였다.

## 4. 주요 문제와 해결 내용

### 가. 최초 로딩 시간이 오래 걸리는 문제

초기에는 앱을 실행할 때마다 문서를 다시 읽고 색인 여부를 확인하는 구조여서 대용량 HWPX와 PDF 자료가 많을 경우 로딩 시간이 길어졌다.

이를 해결하기 위해 앱 시작 시에는 완성된 인덱스만 열도록 변경하고, 문서가 변경된 경우에만 관리자가 별도 재색인을 실행하도록 개선하였다. 또한 인덱스 매니페스트 버전 확인 오류를 수정하여 기존 인덱스를 정상적으로 재사용하도록 하였다.

### 나. 법령 또는 업무절차 일부만 답변되는 문제

초기에는 청크 크기가 작고 상위 5개 결과만 사용하여, 법령의 일부 항 또는 업무절차의 일부 단계만 답변되는 문제가 있었다.

이에 청크 크기를 1,800자로 확대하고, 법령의 `제○조` 단위를 가능한 한 유지하도록 개선하였다. 또한 상위 검색 청크의 앞뒤 인접 청크를 함께 답변 근거로 제공하여 법령 조항과 업무절차의 연속성을 높였다.

### 다. 관계 법령명은 나오지만 답변이 나오지 않는 문제

문서명이나 목차는 검색되었으나 실제 답변에 필요한 본문 근거가 충분하지 않아 "등록된 업무자료에서 확인할 수 없습니다"라는 답변이 나오는 경우가 있었다.

이를 해결하기 위해 검색 후보를 넓게 확보하고, 인접 본문 청크를 추가로 제공하도록 하였다. 또한 근거가 일부만 있는 경우에는 확인 가능한 내용부터 답하고, 부족한 부분만 별도로 표시하도록 답변 지침을 조정하였다.

### 라. ChromaDB 검색 DB 오류

대량 벡터를 저장한 뒤 ChromaDB의 HNSW 인덱스를 다시 열지 못하는 오류가 발생하였다.

이에 ChromaDB 저장 후 종료·재열기 검증 절차를 추가하고, 동일한 임베딩 벡터를 `fallback_vectors.json`에도 저장하도록 개선하였다. 따라서 ChromaDB가 열리지 않는 경우에도 JSON 기반 대체 벡터 검색으로 시스템을 계속 사용할 수 있도록 보완하였다.

### 마. Cloudflare 무료 한도 소진

여러 차례 전체 색인을 수행하는 과정에서 Cloudflare Workers AI 무료 일일 한도 10,000 neurons가 소진되어 답변 생성 시 429 오류가 발생하였다.

이는 코드 오류가 아니라 외부 AI 서비스 사용량 제한이다. 이미 벡터 인덱스가 생성된 이후에는 문서를 변경하지 않는 한 재색인을 반복하지 않고, 기존 인덱스를 재사용해야 한다. 장기적으로는 유료 플랜, 다른 API 또는 로컬 임베딩 모델 도입을 검토할 필요가 있다.

## 5. 구축 및 검증 결과

- 전체 입력 파일: 66개
- 처리 문서: 56개
- 생성 청크: 2,929개
- 중복 제외: 380개
- 처리 실패 문서: 5개
- 대체 벡터 파일: `fallback_vectors.json` 생성 완료
- 자동 테스트 결과: 26개 테스트 통과
- `압류금지재산 알려줘` 질문에서 9개 근거 청크 검색 확인
- 실제 답변 생성 시 5개 근거 문서와 9개 근거 청크 사용 확인

## 6. 현재 운영 방법

평상시에는 아래 명령으로 앱을 실행한다.

```powershell
python -m streamlit run app.py
```

문서를 추가·수정·삭제한 경우에만 아래 명령으로 재색인한다.

```powershell
python -m scripts.build_index
```

Cloudflare 무료 한도 소진을 방지하기 위해, 문서 변경이 없는 경우에는 재색인을 실행하지 않는다.

## 7. 향후 개선 방향

향후에는 다음 기능을 단계적으로 추가할 수 있다.

1. HWPX·PDF의 목차, 표지, 머리말·꼬리말 제거 및 표 구조화
2. 스캔 PDF 대상 OCR 적용
3. 변경된 문서만 처리하는 증분 색인
4. 대표 질문을 활용한 검색·답변 품질 평가
5. 법령 시행일·문서 발행일 표시 및 최신성 경고
6. 담당자 검토 확인 기능

## 8. 결론

본 사업은 OpenAI 유료 API 기반의 일반 민원 RAG 아이디어를 Cloudflare Workers AI 무료 할당 기반으로 전환하고, 이를 징수과 내부 업무매뉴얼 RAG로 구체화한 사례이다. 색인 재사용, 법령 단위 청킹, 인접 청크 보강, 근거 중심 답변, 대체 벡터 저장소를 적용하여 행정업무에 적합한 검색·답변 기반을 마련하였다.

AI 답변은 행정처분이나 법적 판단을 대신하지 않으며, 최종 업무 판단은 담당자가 최신 법령·지침·개별 사실관계를 확인하여 수행해야 한다.
