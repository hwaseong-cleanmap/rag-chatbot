# 민원 RAG에서 징수과 업무매뉴얼 RAG로의 전환 및 개선 보고서

## 1. 추진 개요

### 1.1 프로젝트 목적

본 프로젝트는 민원·법령·업무매뉴얼·지침 등 행정업무 자료를 직원이 자연어로 질문하면, 등록된 문서의 근거를 찾아 답변하는 내부 업무지원 RAG(Retrieval-Augmented Generation, 검색증강생성) 시스템을 구축하는 것을 목적으로 한다.

초기에는 일반 민원자료 중심의 RAG 형태로 검토하였으나, 실제 활용 대상을 화성시 징수과의 체납처분·압류·공매·관허사업 제한 등 업무로 구체화하였다. 이에 따라 프로젝트의 중심을 일반 민원 응답에서 **징수과 업무매뉴얼 및 관계 법령 검색·업무보조 RAG**로 전환하였다.

### 1.2 유료 AI API에서 무료 할당 AI로의 전환

초기 민원 RAG 챗봇은 OpenAI 유료 API를 사용하도록 설계되었다. 문서 임베딩에는 `text-embedding-3-small`, 답변 생성에는 `gpt-5-mini`를 사용하였다. 이 방식은 성능과 사용 편의성 측면의 장점이 있었으나, 문서를 재색인하거나 질문을 반복할수록 API 비용이 발생한다는 운영상 부담이 있었다.

교육·연구 및 내부 프로토타입 단계에서 비용 부담을 낮추기 위해, AI 제공자를 Cloudflare Workers AI로 전환하였다. 전환 후 문서 임베딩에는 `@cf/baai/bge-m3`, 답변 생성에는 `@cf/qwen/qwen3-30b-a3b-fp8`을 사용하도록 변경하였다.

| 구분 | 초기 구성 | 전환 후 구성 |
| --- | --- | --- |
| AI 제공자 | OpenAI 유료 API | Cloudflare Workers AI 무료 할당 기반 |
| 문서 임베딩 | `text-embedding-3-small` | `@cf/baai/bge-m3` |
| 답변 생성 | `gpt-5-mini` | `@cf/qwen/qwen3-30b-a3b-fp8` |
| 인증 정보 | `OPENAI_API_KEY` | Cloudflare Account ID + Workers AI API Token |
| API 주소 | OpenAI 기본 API | Cloudflare OpenAI 호환 REST API |
| 장점 | 비교적 높은 답변 성능과 단순한 사용 | 초기 비용 부담 감소, 무료 할당 범위에서 실습 가능 |
| 유의사항 | 사용량에 따른 비용 발생 | 무료 일일 한도, 모델 성능·처리량 제약 존재 |

Python 코드에 `openai` 패키지가 남아 있는 것은 OpenAI 유료 서비스를 계속 사용하기 때문이 아니라, Cloudflare가 제공하는 **OpenAI 호환 API 형식**으로 요청을 전송하기 위한 클라이언트 라이브러리 사용 때문이다. 실제 모델 호출과 과금·할당 관리는 Cloudflare Workers AI 기준으로 이루어진다.

### 1.3 AI 전환에 따른 설계 변경

- `.env` 환경변수를 `OPENAI_API_KEY` 중심에서 `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN` 중심으로 변경하였다.
- 임베딩과 답변 모델명을 Cloudflare 모델 식별자(`@cf/...`)로 변경하였다.
- Cloudflare의 무료 사용량 한도를 고려하여, 불필요한 재색인을 줄이고 기존 색인을 재사용하는 구조를 중요 개선 과제로 설정하였다.
- 무료 할당 소진 시 답변 생성이 중단될 수 있으므로, 운영 단계에서는 유료 플랜·다른 API·로컬 모델 등 대안을 검토할 수 있도록 하였다.

### 1.4 업무 대상 전환 방향

| 구분 | 초기 방향 | 전환 후 방향 |
| --- | --- | --- |
| 주 사용자 | 민원인 또는 일반 사용자 | 징수과 내부 담당자 |
| 주요 자료 | 민원 FAQ, 민원처리지침 | 업무매뉴얼, 법령, 지침, 판례·해석자료 |
| 답변 목적 | 일반 안내 | 업무 절차·근거 확인 보조 |
| 안전 원칙 | 일반 민원 안내 | 법적 판단·행정처분은 담당자 최종 검토 |
| 핵심 화면 | 질문·답변 | 질문·답변·근거자료·검색된 원문 확인 |

### 1.5 행정업무 적용 원칙

- AI는 담당자를 대체하지 않고 문서 검색과 답변 초안 작성을 보조한다.
- 행정처분, 자격판단, 법적 판단, 개별 체납자 판단은 담당자가 최종 확인한다.
- 답변은 등록된 문서 근거에 한정하며, 근거 없는 일반 지식 보완을 금지한다.
- 문서 및 질문에 포함될 수 있는 개인정보는 마스킹·차단한다.

## 2. 구축한 시스템 구조

```text
PDF / HWPX / PPTX / TXT / RTF-DOC
        ↓
텍스트 추출 및 개인정보 형식 마스킹
        ↓
법령·절차 단위 청킹
        ↓
Cloudflare BGE-M3 임베딩
        ↓
ChromaDB + JSON 대체 벡터 저장소
        ↓
질문 임베딩 및 유사 근거 검색
        ↓
인접 청크 보강
        ↓
Cloudflare Qwen 답변 생성
        ↓
답변 + 근거자료 + 검색 원문 표시
```

### 2.1 주요 구성

- 화면: Streamlit 기반 대화형 웹 화면
- 문서 처리: PDF, HWPX, PPTX, TXT, RTF-DOC 텍스트 추출
- 임베딩 모델: Cloudflare Workers AI `@cf/baai/bge-m3`
- 답변 모델: Cloudflare Workers AI `@cf/qwen/qwen3-30b-a3b-fp8`
- 벡터 검색: ChromaDB
- 안정성 보완: `fallback_vectors.json` 기반 로컬 대체 벡터 검색
- 데이터 저장: `data/` 원본 자료, `vector_db/` 생성 인덱스

### 2.2 색인 대상 자료

자료는 다음과 같이 분류하여 관리하였다.

- 법령: 지방세징수법, 지방세기본법, 시행령·시행규칙, 예규 등
- 매뉴얼 모음: 징수과 업무매뉴얼, 공매 매뉴얼, 압류·추심 자료 등
- 지침: 민원처리지침, 행정안전부 지침, FAQ 등
- 법령해석 및 판례: 유권해석, 판결문, 참고자료 등

## 3. 주요 문제 분석

## 3.1 유료 AI 비용 부담과 무료 할당 전환의 한계

### 전환 배경

초기 OpenAI 유료 API 구조는 문서 임베딩과 답변 생성이 모두 사용량 기반 비용으로 연결되었다. 특히 수십 개의 PDF·HWPX 업무자료를 반복 색인하는 RAG 구조에서는 임베딩 비용을 예측·관리할 필요가 있었다.

이에 Cloudflare Workers AI의 무료 할당을 활용하여 프로토타입 및 교육·연구 단계에서 별도 유료 API 결제 부담을 낮추는 방향으로 전환하였다.

### 전환 결과와 한계

Cloudflare 모델로 임베딩·답변 생성이 실제 동작하는 것을 확인하였다. 다만 무료 서비스는 무제한 서비스가 아니며, 반복 색인 과정에서 무료 일일 한도가 소진되어 429 오류가 발생하였다. 따라서 무료 전환은 비용을 없애는 방식이 아니라, **소규모 실습·프로토타입에 적합한 무료 할당을 활용하는 방식**으로 이해해야 한다.

정식 운영에서는 다음 중 하나를 선택해야 한다.

1. Cloudflare 유료 플랜으로 전환하여 사용량을 관리한다.
2. 로컬 임베딩 모델을 사용하여 문서 색인 비용을 줄인다.
3. 답변 생성 모델을 조직에서 허용하는 별도 상용 또는 로컬 LLM으로 교체한다.

## 3.2 최초 로딩이 지나치게 오래 걸리는 문제

### 원인

초기 앱은 시작 시 `ensure_index()`를 호출하여 자료 변경 여부를 확인하고 필요 시 색인을 수행하도록 구성되어 있었다. 이 과정에서 대용량 HWPX/PDF를 다시 읽고, 개인정보 마스킹·청킹·임베딩을 수행할 가능성이 있었다.

또한 인덱스 매니페스트를 저장할 때는 `index_version`을 사용했으나, 캐시 확인 시에는 `version`을 확인하는 불일치가 있었다. 이로 인해 기존 인덱스를 재사용하지 못하고 문서 본문을 다시 읽는 문제가 발생했다.

### 해결

- 앱 시작 시 전체 색인이 아닌 `load_ready_index()`만 실행하도록 변경하였다.
- 색인은 관리자 실행 명령 또는 화면의 명시적 재색인 기능에서만 수행하도록 분리하였다.
- 매니페스트 확인 키를 `index_version`으로 통일하였다.
- 문서 경로·크기·수정 시각만 비교하여 자료가 변하지 않은 경우 기존 색인을 재사용하도록 보완하였다.

### 결과

정상 인덱스가 생성된 후에는 앱 시작 시 원본 HWPX/PDF를 다시 읽거나 임베딩 API를 다시 호출하지 않고, 생성된 로컬 색인을 바로 열도록 개선되었다.

## 3.3 법령 또는 절차의 일부만 답변되는 문제

### 증상

`압류금지재산` 질의에 법령의 첫 항 또는 일부 항목만 답변되고, `자동차 공매 절차`와 같은 질문에서는 절차 전체가 충분히 답변되지 않는 현상이 확인되었다.

### 원인

- 초기 청크 크기 1,200자와 상위 검색 결과 5개는 긴 법령 조문·절차 매뉴얼의 전체 흐름을 담기에 부족할 수 있었다.
- 벡터 검색은 각 청크를 독립적으로 평가하므로, 가장 관련성 높은 한 청크만 검색되고 바로 앞뒤의 이어지는 청크는 누락될 수 있었다.
- 목차·표지·제목처럼 키워드 밀도가 높은 조각이 실제 본문보다 상위에 검색될 가능성이 있었다.
- "근거가 부족하면 지정 문구만 답하라"는 엄격한 프롬프트 때문에, 일부 근거가 있어도 모델이 전체 답변을 포기할 가능성이 있었다.

### 해결

- 기본 청크 크기를 1,800자, 중첩 구간을 300자로 확대하였다.
- 법령 문서에서는 `제○조`를 우선 인식하여 한 조문과 그 항목이 가능한 한 같은 청크에 유지되도록 개선하였다.
- 검색 후보 수를 상위 답변 청크 수보다 넓게 확보한 뒤, 실제 답변에는 상위 청크와 해당 청크의 앞뒤 인접 청크를 함께 제공하도록 개선하였다.
- 답변 컨텍스트는 최대 10개 청크로 제한하여 비용과 응답 지연을 관리하였다.
- 근거가 일부만 있을 때에는 확인 가능한 내용을 먼저 답하고, 부족한 부분만 별도로 표시하도록 답변 지침을 조정하였다.

### 결과

실제 `압류금지재산 알려줘` 검색에서 9개 근거 청크가 반환되었고, 답변 생성 시 5개 문서와 9개 근거 청크가 활용되는 것을 확인하였다.

## 3.4 관계 법령명은 표시되지만 답변이 "확인할 수 없음"으로 나오는 문제

### 원인

화면의 근거자료 카드는 검색된 문서명을 보여준다. 따라서 문서 제목·목차·일부 조항이 검색되어도, 질문 전체를 답할 충분한 본문 근거가 제공되지 않으면 답변 모델은 안전하게 "등록된 업무자료에서 확인할 수 없습니다"라고 반환할 수 있다.

즉, **근거자료 카드가 표시되는 것**과 **답변에 필요한 충분한 본문 근거가 확보된 것**은 서로 다르다.

### 해결

- 인접 청크 보강을 적용하여 검색된 문서명의 주변 실제 본문을 함께 제공하였다.
- 답변 프롬프트에서 일부 확인 가능한 내용은 답변하도록 조정하였다.
- NO_ANSWER 문구가 답변에 포함되기만 해도 전체 답변을 폐기하던 로직을, 정확히 그 문구만 반환된 경우에만 NO_ANSWER로 처리하도록 개선하였다.

## 3.5 코드 구조 오류

### 증상

`_stats`, `document_stats`, `search`, `answer` 메서드가 `RagService` 클래스가 아니라 `build_index_atomically()` 함수 내부에 잘못 들여쓰기된 상태가 확인되었다.

### 해결

해당 메서드를 `RagService` 클래스 내부로 복구하고, 메서드 존재 여부를 확인하는 회귀 테스트를 추가하였다.

### 결과

검색·답변·통계 기능이 서비스 객체의 정상 메서드로 동작하도록 복구되었다.

## 3.6 ChromaDB HNSW 인덱스 오류

### 증상

색인 생성 후 다음 오류가 반복되었다.

```text
IndexStatusError: 검색 DB를 열 수 없습니다. 관리자가 색인을 다시 생성해야 합니다.
Error loading hnsw index
```

### 원인

Windows 환경에서 ChromaDB 1.5의 대용량 HNSW 벡터 파일이 정상적으로 기록되지 않는 현상이 확인되었다. SQLite 파일과 매니페스트는 생성되었지만, 재시작 후 HNSW 검색 인덱스를 읽지 못하는 문제가 발생하였다.

### 해결

1. 색인 완료 후 ChromaDB 클라이언트를 명시적으로 종료하고 재열기 검증을 수행하도록 변경하였다.
2. ChromaDB 1.5의 `configuration` 방식으로 HNSW 저장 동기화 설정을 적용하였다.
3. ChromaDB 오류에 대비하여 동일한 임베딩과 메타데이터를 `fallback_vectors.json`에도 저장하도록 구현하였다.
4. 앱 실행 시 ChromaDB를 열 수 없으면 `fallback_vectors.json`을 자동으로 읽어 코사인 유사도 검색을 수행하도록 구현하였다.

### 결과

최종적으로 다음 파일이 생성되었다.

```text
vector_db/
├─ chroma.sqlite3
├─ index_manifest.json
└─ fallback_vectors.json
```

`fallback_vectors.json`은 약 64MB로 생성되었으며, 2,929개 청크를 포함한다. ChromaDB의 HNSW 파일 상태와 무관하게 로컬 대체 검색이 가능해졌다.

## 3.7 Cloudflare Workers AI 무료 한도 소진

### 증상

답변 생성 단계에서 다음 오류가 발생하였다.

```text
Error code: 429
you have used up your daily free allocation of 10,000 neurons
```

### 원인

여러 차례의 전체 색인 과정에서 대량 문서의 임베딩 요청이 반복되어 Cloudflare Workers AI 무료 일일 한도 10,000 neurons가 소진되었다. 이후 답변 생성 모델 호출도 같은 한도의 영향을 받아 429 오류가 발생하였다.

### 현재 상태와 대응

- 이 오류는 시스템 코드 오류가 아니라 외부 서비스 사용량 한도 문제이다.
- 인덱스 파일은 이미 생성되어 있으므로, 한도 복구 전까지 재색인을 반복해서는 안 된다.
- 한도 복구 후에는 기존 인덱스를 이용하므로 질문당 임베딩 1회와 답변 생성만 수행된다.
- 장기 운영을 위해서는 Cloudflare 유료 플랜 전환, 별도 API 사용 또는 로컬 임베딩·로컬 모델 도입을 검토할 수 있다.

## 4. 검증 결과

### 4.1 자동 테스트

다음 명령으로 자동 테스트를 수행하였다.

```powershell
python -m pytest -q --basetemp .pytest_tmp
```

결과:

```text
26 passed, 1 warning
```

### 4.2 색인 및 검색 확인

- 처리 문서: 56개
- 전체 입력 파일: 66개
- 생성 청크: 2,929개
- 중복 제외: 380개 텍스트 단위
- 처리 실패: 5개 파일
- 대체 벡터 저장소: `fallback_vectors.json` 생성 완료
- `압류금지재산 알려줘` 검색: 9개 근거 청크 반환 확인
- 실제 답변 생성: 5개 근거 문서, 9개 근거 청크 사용 확인

### 4.3 처리 실패 문서 유의사항

처리 실패한 일부 PDF는 스캔형 문서, 글꼴 인코딩 문제 또는 텍스트 추출 제한이 원인일 수 있다. 해당 문서까지 검색 대상으로 포함하려면 OCR(광학문자인식) 적용 또는 텍스트 기반 PDF 원본 확보가 필요하다.

## 5. 운영 방법

### 5.1 평상시 앱 실행

```powershell
python -m streamlit run app.py
```

정상 운영 시 앱은 기존 `vector_db`를 즉시 열며, 원본 문서를 다시 읽거나 전체 임베딩을 수행하지 않는다.

### 5.2 문서가 변경된 경우에만 재색인

```powershell
python -m scripts.build_index
```

재색인은 문서를 추가·수정·삭제했을 때만 실시한다. Cloudflare 무료 한도 및 처리 시간을 고려하여 불필요한 반복 실행은 피해야 한다.

### 5.3 운영 점검 항목

- `vector_db/index_manifest.json` 존재 여부
- `vector_db/fallback_vectors.json` 존재 여부
- 문서 추가·수정 후에만 재색인 실행 여부
- Cloudflare Workers AI 한도 및 과금 상태
- 처리 실패 문서의 OCR 필요 여부
- 답변 근거자료와 최신 법령·지침의 일치 여부

## 6. 향후 개선 과제

1. HWPX·PDF 2차 전처리 도입
   - 목차, 표지, 머리말·꼬리말 제거
   - 표를 항목형 텍스트로 구조화
   - 절차 제목·단계·법령 조항을 더 정확히 분리

2. OCR 적용
   - 스캔 PDF와 인코딩 문제 PDF를 텍스트화하여 검색 대상 확대

3. 재색인 비용 절감
   - 변경된 문서만 임베딩하는 증분 색인 방식
   - 로컬 임베딩 모델 또는 별도 임베딩 API 검토

4. 검색 품질 평가 체계
   - 대표 업무 질문 목록 구축
   - 질문별 기대 근거 문서·조문·절차 정의
   - 검색 성공률, 답변 완결성, 담당자 만족도 측정

5. 행정 안전장치 강화
   - 답변별 법령 시행일·문서 발행일 표시
   - 법령과 매뉴얼 간 상충 경고
   - 담당자 검토 확인 기능

## 7. 결론

본 프로젝트는 OpenAI 유료 API 기반의 일반 민원 RAG 아이디어를 Cloudflare Workers AI 무료 할당 기반으로 전환하고, 다시 징수과 내부 업무지원 도구로 구체화하였다. 구축 과정에서 AI 제공자 전환, 초기 로딩 지연, 불완전한 법령·절차 답변, 근거 부족 응답, 코드 들여쓰기 오류, ChromaDB 영속 인덱스 오류, Cloudflare 무료 한도 소진 등의 어려움이 발생하였다.

이에 인덱스 재사용 구조, 법령 단위 청킹, 인접 청크 보강, 답변 지침 개선, 코드 구조 복구, JSON 기반 대체 벡터 저장소를 적용하였다. 그 결과 2,929개 청크의 업무자료 인덱스와 실제 검색·답변 흐름을 구축하였다.

향후에는 문서 전처리 고도화, OCR, 증분 색인, 검색 품질평가, 운영비용 관리 등을 통해 실제 행정 현장에서 안정적으로 활용할 수 있는 업무지원 시스템으로 발전시킬 필요가 있다.
